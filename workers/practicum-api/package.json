{
  "name": "practicum-api",
  "version": "0.1.0",
  "private": true,
  "description": "Practicum CLI license API — Dodo webhook, KV-backed license keys, validation endpoint",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
